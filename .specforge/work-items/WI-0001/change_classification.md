# 变更分类 — WI-0001 飞检现场管理系统

## 分类结论

**变更类型**：`new_product`

这是从零构建的全新独立产品，仓库根目录除 `doc/`（业务输入）和 `.specforge/`（治理配置）外为空，无存量代码、无存量数据库 schema、无存量接口契约需要兼容。

## 影响范围性质

- **向后兼容性**：不适用（全新系统）
- **历史数据迁移**：不适用（全新数据库）
- **存量接口契约**：不适用（全新 API 设计）
- **存量权限模型**：不适用（全新 RBAC）

## 唯一需要"兼容"的内容

`doc/飞检现场管理系统_业务逻辑定稿_v1.14_最终干净版_开发输入.md`（9192 行）作为唯一业务输入，后续所有需求/设计/任务拆解必须与之闭合。文档自身 §0.3 已声明：
> 如果页面、字段、状态机、权限、项目配置之间出现不闭合，必须先修正文档再编码。

这意味着实现层必须反向校验文档闭合性，但不存在"已有代码需要适配"的影响。
