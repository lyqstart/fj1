# FJ API 生产环境变量 — 复制为 .env 并填入真实值
# 权限设置：chmod 600 .env
# 不要提交到 Git

# JWT 密钥（至少 32 字符，建议用 openssl rand -base64 48 生成）
JWT_SECRET=

# 数据库密码
DB_PASSWORD=

# 数据库用户（默认 fj_app）
DB_USER=fj_app

# CORS 允许的前端来源
CORS_ALLOWED_ORIGINS=http://your-server-ip

# Spring profile
SPRING_PROFILES_ACTIVE=prod
